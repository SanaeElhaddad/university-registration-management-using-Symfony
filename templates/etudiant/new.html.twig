{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block stylesheets %}
    {# Include your CSS stylesheets here #}
    <link rel="stylesheet" href="{{ asset('assets/plugins/bootstrap/css/bootstrap.min.css') }}" />
    <link rel="stylesheet" href="{{ asset('assets/css/style.min.css') }}" />
    <link rel="stylesheet" href="{{ asset('assets/css/default.css') }}" />
    <!--link rel="icon" href="favicon.ico" type="image/x-icon"/-->
    
    <style>
        body {
            background-color: #f8f9fa;
            margin: 0; /* Remove default margin */
            padding: 0; /* Remove default padding */
        }

        .auth {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 210vh; /* Adjusted height */
        }

        .auth_left {
            width: 40%;
            margin: 20px;
            padding: 20px;
            border: 2px solid #00008B; /* Dark blue border */
            border-radius: 10px;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .card-title {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        form {
            margin-top: 20px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            font-weight: bold;
            margin-bottom: 5px;
        }

        input {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 14px;
        }

        .custom-checkbox {
            margin-top: 10px;
        }

        .custom-width {
            width: 100%;
            height: 120px;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            padding: 10px 20px; /* Added padding */
            font-size: 18px; /* Increased font size */
        }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
        }

        .text-muted {
            color: #6c757d;
        }

        a {
            color: #007bff;
        }
    </style>
{% endblock %}

{% block body %}
    <body class="font-muli rtl">

        <div class="auth option2">
            <div class="auth_left">
                <div class="card">
                    <div class="card-body">
                        <h2 style="text-align: center; color: #00008B; font-weight: bold;">Inscription</h2>
                        {{ include('etudiant/_form.html.twig') }}
                        <form>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        {# Include your JS scripts here #}
        <script src="{{ asset('assets/bundles/lib.vendor.bundle.js') }}"></script>
        <script src="{{ asset('assets/js/core.js') }}"></script>
    </body>
{% endblock %}
