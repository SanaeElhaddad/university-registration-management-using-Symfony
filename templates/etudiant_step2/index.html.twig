{% extends 'base.html.twig' %}

{% block title %}EtudiantStep2 index{% endblock %}

{% block body %}
    <h1>EtudiantStep2 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Niveau</th>
                <th>Note_math</th>
                <th>Note_franc</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for etudiant_step2 in etudiant_step2s %}
            <tr>
                <td>{{ etudiant_step2.id }}</td>
                <td>{{ etudiant_step2.niveau }}</td>
                <td>{{ etudiant_step2.noteMath }}</td>
                <td>{{ etudiant_step2.noteFranc }}</td>
                <td>
                    <a href="{{ path('app_etudiant_step2_show', {'id': etudiant_step2.id}) }}">show</a>
                    <a href="{{ path('app_etudiant_step2_edit', {'id': etudiant_step2.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_etudiant_step2_new') }}">Create new</a>
{% endblock %}
